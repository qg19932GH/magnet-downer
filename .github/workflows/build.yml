name: Build JavBusCrawler

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Build with PyInstaller
        run: pyinstaller JavBusCrawler.spec

      - name: Upload executable
        uses: actions/upload-artifact@v4
        with:
          name: JavBusCrawler
          path: dist/JavBusCrawler.exe

      - name: Create Release
        if: github.event_name == 'push'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: build-${{ github.run_number }}
          name: JavBus 磁力链接极速抓取器 构建 ${{ github.run_number }}
          body: |
            自动构建产物（PyInstaller）
            版本: v2.6
            包含修复: selenium 依赖、线程安全、正则优化等
          files: dist/JavBusCrawler.exe
