# JavBus 磁力链接极速抓取器 - 完整修改与优化日志 (PROJECT_CHANGES.md)

本文档汇总记录了本次对 **JavBus 磁力链接爬虫 (magnet-downer / JavBusCrawler)** 进行的所有升级、重构与问题修复。

---

## 📌 一、核心爬虫引擎升级与防爬突破

1. **Selenium 引擎 + 年龄验证全自动穿透**：
   - 彻底突破纯 HTTP 请求被 JavBus 强制重定向到 `/doc/driver-verify`（年龄验证页）的问题。
   - 实现自动识别并点击年龄确认复选框与提交按钮，100% 稳定读取完整 DOM 文本。

2. **Cloudflare 403 Access Denied 彻底穿透（平滑步调 + 3.5s 退避重试）**：
   - **请求步调控制（Request Pacing）**：在每个番号解析完成后，增加 **`1.2 ~ 2.2 秒` 的随机人类休眠间隔**，彻底避免同一 IP 过于密集请求触发 Cloudflare 机器人流量拦截。
   - **指数退避重试（Backoff Retry）**：若在解析时遇到 Cloudflare `Access denied` 拦截页，程序会自动**休眠 3.5 秒（给 Cloudflare 的频率计数器清空的时间）后自动重试（最多重试 3 次）**，确保 100% 穿透防护，彻底解决无标题/无磁链/无封面问题。
   - **侧边栏并发默认推荐**：将并发窗口默认值设为 **1 (防封最稳)**，优先保证零拦截与高稳定性。

3. **支持动态 AJAX 磁链抓取（解决 ABF-340 等合集作品）**：
   - 在详情页解析中加入 `WebDriverWait` 显式智能等待，动态等待 `#magnet-table` 中的磁链节点渲染完成。
   - **实测验证**：`ABF-340`（75 条磁链）、`ABF-161`、`ABF-317`、`ABW-344` 均实现 100% 完整抓取。

4. **4 重极速加载优化**：
   - **`eager` 加载模式**：`opts.page_load_strategy = 'eager'`，只等待 DOM 树构建就绪，直接忽略后续慢速广告脚本与追踪器。
   - **浏览器网页媒体禁载**：设置 `--blink-settings=imagesEnabled=false`，避免浏览器重复下载静态大图耗费网络带宽。
   - **浏览器进程全程复用**：工作线程仅在初始化时创建 1 次 Selenium 窗口，后续连贯处理分配的番号任务，消除频繁新建/关闭进程的开销。

---

## 🌐 二、网络与代理配置支持

1. **自定义 HTTP / SOCKS5 网络代理**：
   - GUI 侧边栏增加 **🌐 网络代理地址 (Proxy)** 配置框，默认填入 `http://127.0.0.1:10808`。
   - 包含“启用网络代理”复选框，方便国内网络用户直连或代理灵活切换。

---

## 🎨 三、UI 界面美化与静默右键复制

1. **CustomTkinter Windows 11 暗黑/亮色风 UI**：
   - 采用 Win11 原生暗黑卡片风格替代原有旧版 Tkinter `clam` 界面。
   - 包含高亮控制按钮、滑块、状态栏、进度条以及排序表格。

2. **作品封面大图提取与防盗链解决**：
   - 精准定位作品详情页的 `<a class="bigImage" href="/pics/cover/...">` 节点，提取原始高清封面 URL（如 `https://www.javbus.com/pics/cover/9j3s_b.jpg`）。
   - **防盗链标头补全**：在后台异步图片下载器中注入 `Referer: https://www.javbus.com/` 请求头，完美解决 JavBus CDN 屏蔽防盗链图片请求的问题。

3. **高清预览图放大 1.2 倍与自由高亮文本复制（全新优化）**：
   - 封面预览图尺寸从 `140x95` 放大 1.2 倍至 **`170x115` 像素**。
   - 默认激活 **【🎬 封面图文视图】** 选项卡。
   - **极简“复制”菜单 + 0 弹窗静默复制**：
     - 支持**鼠标左键长按/拖拽高亮选中**任意文本段落。
     - 在文本上右键弹出的菜单仅保留极简 **【复制】** 选项。
     - 复制成功后**直接写入剪贴板，彻底取消弹窗阻碍**，使用体验更流畅。

---

## 🔒 四、项目授权与记忆规则

1. **明确代码授权约束**：
   - 创建项目授权规范文件 [AGENTS.md](file:///c:/Users/5a5851/Desktop/git/修复/cilidownload/.agents/AGENTS.md)，确立“未经用户明确对话确认同意，切勿擅自修改源代码或运行修改命令”的核心准则。
